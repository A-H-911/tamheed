# Tamheed MCP server

Documents the tool surface as of **tamheed v4.11.0**.

The **only write path** into a Tamheed package (ADR-0001). Agents interact with a package
exclusively through these MCP tools: every write passes schema validation (FKs, CHECKs,
NOT NULL provenance), queries return rows instead of whole documents, and a constraint
violation surfaced by a tool *is* the quality-gate report. This server is the successor of
`validate_package.py` — the mechanical half of the capability the skill owns.

## Install & launch

**Python floor: 3.10** (the `mcp` SDK's `requires-python = ">=3.10"`, verified 2026-07-17
from its pyproject; program decision ASM-D raises the repo floor to match).

Plugin installs copy files only — they do not install Python packages. Two launch paths:

1. **`uv` (preferred, zero setup):** the server script carries PEP 723 inline metadata, so
   `uv run tamheed_server.py` fetches the SDK automatically. The bundled `.mcp.json` at the
   plugin root uses this path (`${CLAUDE_PLUGIN_ROOT}`-relative) and auto-starts the server
   when the plugin is enabled.
2. **pip fallback:** `pip install "mcp<2"`, then `python tamheed_server.py --package-dir <root>`.

If the SDK is missing the server exits with a one-line error naming both options — never a
silent dead server.

`--selftest` lists the tools and, when the `mcp` SDK is importable, registers every tool
with FastMCP and reports `N/N tools registered` (exit 1 on any registration failure).
`--package-dir <root>` sets the directory packages live under. Resolution is layered
(field-evidence C11 — a stdio server's cwd is not guaranteed): explicit flag >
`CLAUDE_PROJECT_DIR` (exported by Claude Code to plugin server processes) > cwd; an
unexpanded `${...}` literal counts as unset. Every `package_*` result echoes the resolved
absolute root, and `server_info` reports it on demand.

## Rules

- **No raw SQL, ever.** Tools take structured, validated arguments; unknown types/columns
  are rejected by name.
- **One external dependency, bounded (C33).** Everything is stdlib except the MCP SDK,
  pinned `mcp>=1.2,<2` in the PEP 723 header — SDK 2.0.0 removed `mcp.server.fastmcp`
  and an unbounded resolve broke every fresh environment while cached ones kept working.
  `--selftest` reports SDK serving status (`mcp sdk: ok (<version>)`) without requiring
  it; the pin is never widened without a deliberate port to the SDK's successor module.
- **Single writer.** Opening a package takes `data/.lock` (from `db/store.py`); a second
  opener fails loud, and the lock names who holds it and since when (C31: a bare PID
  invited an unsound liveness check — the OS reuses PIDs). The lock releases on
  `package_close` or process exit.
- **The working tree is the truth (C31).** Canonical `data/` lives in the project's git
  working tree: uncommitted package writes are destroyed by `git reset --hard` /
  `checkout` / `stash` like any uncommitted change — commit package data before branch
  operations. Writes refuse to overwrite a tree that moved underneath the open session
  (`StoreStaleError`: batch NOT applied; close, reconcile via git, reopen), and the
  append-only journal (`progress_entries`, `audit_verdicts`) rejects in-place rewrites —
  corrections are appended, never edited.
- **Batch-first.** Mutation tools accept arrays and apply them in ONE transaction,
  all-or-nothing, with per-item verdicts naming any violated constraint.
- **Stored text is data, never instructions.** Brief-derived text is inert;
  `handoff_emit` runs a G-INJECT-style screen and refuses to emit instruction-shaped text.
  An `entity_export` file carries the same brief-derived text for a script to render —
  the script escapes it (the viewer's escape-first rule applies to consumers too).
- **Reads go through the tools too.** A committed script that must quote the store reads an
  `entity_export` file the tool wrote — never `data/*.jsonl`, never a pasted display; a
  full-row status flip names its untouched columns (`expect_unchanged`) and the store
  refuses transport drift.
- **Cascade-on-transition (C4).** State transitions (AC verdict → requirement auto-advance,
  edges → gate views) propagate via schema triggers/views in the same transaction — there is
  no "reconcile trackers" commit to forget.

## Tool reference

| Tool | Kind | Summary |
|---|---|---|
| `server_info(detail?)` | read | Server version (from the bundled `plugin.json`), resolved package root, open package, migrations head — makes startup diagnosable (C11/C16) With a package open, `package` is its stored row (name, title, profile, mode, iteration, `package_version`, `go_no_go`, `entry_point` — no other tool reads it; a package resolves by DIRECTORY, the stored name is descriptive). `detail=true` adds `entity_types` (type, table, id prefix) and `relation_rules` (allowed endpoint types per relation) — the vocabulary, without learning it from a refusal (plan 066) The header it reports is written with `entity_upsert(type="package")` (plan 094) |
| `package_create(name, title, profile, mode)` | mutate | Create under the package root; seeds the `entity_types` registry; takes the lock |
| `package_open(name)` | mutate | Open an existing package (takes the lock) A refusal on a held lock reports what the store OBSERVED about the holder — `not-running`, `reused`, `alive` or `unobservable`, with the evidence (plan 063) |
| `package_unlock(name, confirm?)` | read by default; `confirm=true` removes `data/.lock` + 1 journal row | The sanctioned route out of a lock whose holder is gone (findings_25 §1, plan 064). Default = REPORT: the lock, the observation and whether confirm would proceed; nothing written. `confirm=true` is OPERATOR-WORDS-ONLY, like `force`: it proceeds only when the holder was observed `not-running` or `reused` (pid recycled — decided by the process start identity the lock records, never by a bare liveness check); it REFUSES on `alive` and on `unobservable` (another host or pid namespace, access denied, a legacy lock, a platform that cannot report a start time) — removing `data/.lock` by hand stays the deliberate path for what this host cannot see. It first proves the on-disk store loads, removes only the exact lock bytes it judged, then opens, appends one `forced-override` row (actor `system:package-unlock`), and closes |
| `package_close()` | mutate | Write back canonical JSONL, release the lock (every write already flushed canonical JSONL through the store — close releases the lock; it is not the moment data reaches disk) |
| `entity_upsert(entities[])` | mutate | Batch upsert; items are `{"type": ..., <columns>}`; per-item verdicts; all-or-nothing. The **lesson confirm guard** (plan 036): a write landing a lesson in `Approved`/`Promoted` from any other state — a new row born there included — is refused without `"operator_confirm": true` (operator-words-only, the `force` doctrine); content must be byte-identical to the stored row, approval requires non-empty `confirmed_by` on that write, promotion requires stored-`Approved` + `promoted_to` naming an existing `SKL-` row, and the server appends the typed `lesson-confirmed`/`lesson-promoted` journal event itself (actor `system:lesson-guard`). **Edge retire** (v4.6, findings_23 §1): trace edges are keyed `(from_id, to_id, relation)`, so a new relation sits BESIDE an old one — `{"type": "trace-edge", "from_id", "to_id", "relation", "retire": true}` (exactly those keys) DELETES that triple, bypassing the relation rule (a mistyped edge is what gets retired), the server appends a `correction` journal row naming it (actor `system:edge-retire`) in the same transaction, and an absent triple is an error (an attempt is not a write). Retype in ONE batch: the retire item + the corrected edge An UPDATE's per-item result carries `changed_columns` - each `{column, old_len, new_len}` for text - so a re-sent long field that lost a paragraph shows as a length drop (plan 080; JSON columns compare as parsed values, as `expect_unchanged` does). LESSONS (plan 075): approving or promoting a lesson retires every binding lesson whose `superseded_by` names it - same write, one `transition` journal row each, ids returned as `superseded`; on an Approved/Promoted lesson ANY move off a binding status, and any change to `superseded_by`, is refused without `operator_confirm` (what binds on the operator's word is retired on it too) A by-hand lesson retirement (an Approved/Promoted row moved off a binding status on the operator's word) is journaled by the engine as a `transition` row by `system:lesson-guard`, returned as `lesson_audit` (plan 086; the automatic path keeps `system:lesson-supersession`). FEEDBACK (plan 087): an `FB-` row is born Proposed freely; `Confirmed` needs `operator_confirm` + `confirmed_by` and is journaled (`system:feedback-guard`, returned as `feedback_audit`); a `local-tool` kind arriving on any row needs the word and lands it Confirmed; while Confirmed/Reported/Resolved the row's content (`kind`, `title`, `detail`, `workaround`, `tool_path`, `tool_or_rule`, `confirmed_by`) changes only with the word, and leaving that set is journaled too THE HEADER (plan 094): an item `{"type": "package", ...}` writes the one header row of the open package — `title`, `mode`, `iteration` (an integer), `mvp_definition`, `entry_point`, `go_no_go`; identity columns (`name`, `profile`, `package_version`, `created_at`) refused by name; `go_no_go` is the governance verdict and changes only with `operator_confirm`, journaled as `system:package-guard` and returned as `package_audit`; never a family (no register, no CSV, no registry row). THE SUBSTITUTE (plan 095): `{"type", "id", "substitute": {"<column>": ["<old>", "<new>"]}}` (only `operator_confirm`/`expect_unchanged` beside it) changes one token in one column without the whole row passing through your output — the server materializes the stored row, replaces the exact text, and sends the result down the ordinary full-row path, so every guard, trigger, `expect_unchanged` and `changed_columns` judge it as a caller's row; the item reports `substituted` counts. Refused by name: a mixed item, the journal, composite-key rows, `id`, a non-TEXT column, zero occurrences, a JSON column that would stop parsing, and a match glued to a digit (`DEC-20` inside `DEC-208` is never rewritten). THE OPERATOR'S WORD is the JSON boolean `true` and nothing else on every guard (plan 094's security review) |
| `entity_query(type, id?, status?, columns?, limit?, after_id?, ids?, search?)` | read | Targeted rows from one family — token-lean; returns `total` beside the LIMIT'd rows so truncation is never silent, and (v4.5, findings_22 §1) the means to reach past it: `after_id` keyset paging (the cut and the order share one byte collation; the result's `next_after` is the cursor, exact via a one-row look-ahead, null on the last page; `total` counts the filtered set, constant across a walk), `ids` (a known set in one call, in id order — absent ids simply missing), `search` (case-insensitive substring over the family's TEXT columns, `%`/`_` escaped). `limit` cuts ROWS, never fields — there is no field truncation in the query path; a payload cap is the client's Reads announce what they hid (plan 068): with `columns` the result carries `omitted_columns`; with `search` it carries `matched` — `{id: [columns]}` — because `search` is an substring match (case-insensitive for ASCII - SQLite's default `LIKE`) over EVERY text column incl. `custom_attributes`, and a hit in an unprojected column otherwise reads as a fuzzy match. Both are siblings of `rows`; row dicts never change With `context=N` the result carries `occurrences: {id: {column: {count, snippets}}}` — a census: exact counts on the raw needle with `LIKE`'s ASCII case folding, snippets of N characters either side (capped at 5 per column, 50 per response; counts never capped) (plan 092). `type="package"` is refused with a pointer: the header is read with `server_info().package` and written with `entity_upsert(type="package")` (plan 094) |
| `trace_query(entity_id, direction?, relation?)` | read | Traverse typed `trace_edges` (in/out/both) |
| `gate_run()` | read | Referential gates VERIFY at gate time (plan 027: `PRAGMA foreign_key_check`, entity_index consistency, real status/provenance SELECTs — whitespace-only provenance caught); coverage gates run the SQL views; content tier scans placeholders (code spans stripped per the frozen v1 contract; `custom_attributes` exempt as provenance); warns when G-TRACE passes vacuously (0 MVP rows); audit evidence split over each ACTIVE AC's LATEST verdict (the acs-met population — v4.6, findings_23 §2; superseded history never counts) into evidenced / narrated (graded, no evidence — C7) / ungraded (a Pending placeholder), with `narrated_ids` + `ungraded_ids` naming them; the **blocking G-REL gate** fails on stored edges violating RELATION_RULES (migrate cleans at conversion, adopt reports, writes reject); G-PROGRESS warns on its vacuous pass; `[NEEDS-CLARIFICATION: OQ-NNN]` markers are G-COMPLETE-validated (legal only while the cited OQ is live) |
| `readiness_check(scope?, id?)` | read | Deep lifecycle readiness at a close boundary (package/phase/slice): blocking rules (pre-approval decisions/ADRs, ACs not latest-Met, open **critical/high** defects — medium/low advise, undischarged risks, open work incl. the claimed-done `Review` state), liveness advisories (overdue OQs, unowned high risks, unmeasurable hypotheses, unmerged scope changes, unbound ACs, open markers, the DEC→ADR promotion nag, `lessons-confirmed` — Proposed lessons awaiting the operator's confirmation interview, `lessons-note-budget` — the lessons rendering past the always-loaded note's curation ceiling of 20 lines, named in render order as promotion candidates; the `scope-changes-merged` note teaches `amends` merges and Merged-last), operator-approved `WVR-` **waivers** (reported `waived`, never silent; expiring; each waived entity cites the waiver naming it via `applies_to` before a whole-rule one — plan 060), `human_required` from declared `execution_gates` — all four kinds incl. `ready`, with `Go/Hold/Redirect/Kill` outcomes (prose never machine-evaluated). The same blocking rules guard the phase/slice `Implemented` transition in `entity_upsert` (item-level `"force": true` after explicit operator confirmation; the server appends the FORCED `PE-` audit row itself) Every query-built rule reports the `population` it measured (`table`, `rows`, `scoped`) — a verdict without its denominator cannot be told from a rule that had no subject; `lessons-confirmed` reads `indeterminate` on a package with no lessons at all (plan 069). The package-scope advisory `prose-ids-resolve` names identifiers written in PROSE that resolve to no entity — G-IDS checks foreign keys and the index, never a sentence; code spans, JSON keys, Superseded/Obsolete rows and the append-only tables are not scanned (plan 070) A query-built rule whose family holds ZERO rows reads `indeterminate`, never a silent pass - unless the family's omission is RECORDED, a deliberate zero that reads `pass` and carries `omitted` (plan 077). `prose-ids-resolve` also reports `in_code_spans` and `not_well_formed`, informational lists: its entity list is a floor, not a census (plan 076). Two more advisories: `lessons-superseded-binding` (an Approved lesson still binding beside an approved successor - plan 075) and `waivers-open-ended` (whole-rule waivers with no expiry; emitted only when the package has waivers - plan 079) `prose-ids-resolve` treats `_` as a word character (`KPI-17_score` is a variable, not a citation), states its classification order (the three lists are disjoint; width is tested first) and says when ANY list is cut (`in_code_spans: showing 50 of N`); the whole-table `indeterminate` note names `scoped: false` as the tell between it and plan 049's scoped zero (plan 085) `prompt-ids-resolve` (plan 093): an advisory over the PROJECT's prompt files — every `<package>/prompts/*.md` that is not a stock body, current or any older release's — for identifiers that resolve to no entity, the row rule's three lists, caps and doctrine (backticks make a quotation inert; the list is a floor); entities read `prompts/<file>:<line> -> <id>`; `population` counts files (`unit: "files"`); zero project files reads `indeterminate`. A code span may wrap a line in a prose file and the scan honours it |
| `progress_update(entries[])` | mutate | Append TYPED progress events (`event_type`, `subject_id`, `actor`, `corrects` compensation pointer; `PE-` ids auto-assigned; journals corrected, never edited). The server-appended kinds — `forced-override`, `lesson-confirmed`, `lesson-promoted`, `integrity-verified` — are REFUSED here (v4.5: the batch is refused naming the item and the tool that appends the event; a caller-written one would be a narrated record of a mechanical fact) An actor starting with `system:` is REFUSED on both caller paths (here and the `progress-entry` upsert) — `system:<component>` is the engine's own namespace, what the server witnessed; a caller records as `human:<name>` or `agent:<session>` (plan 086, security review) |
| `audit_record(verdicts[])` | mutate | AC verdicts with the evidence chain (`evidence`, `verified_by`, `verification_method`, `against_commit`); cascades auto-advance on LATEST-verdict semantics |
| `work_bind(ref, entity_ids[], note?)` | mutate | "This commit/PR satisfies FR-x/AC-y/SL-z" — stamps `last_referenced` (C3) Bind a sha that is on origin: tamheed cannot see remotes, and a rebase makes a local sha false (a field package bound thirteen rows to an unpushed commit and had to re-bind) |
| `entity_export(path, tool?, args?)` | mutate (a derived file outside the package data) | The sanctioned read for committed scripts (v4.7, findings_24 §1): runs one read-only tool — `entity_query` (default), `trace_query`, `gate_run`, `readiness_check`, `package_verify` (`record` refused), `server_info` — and writes its WHOLE result to `<package>/exports/<path>` (absolute paths allowed; refused under `data/`, resolved first; an existing file is replaced only if it is itself an export). Envelope `{"tamheed_export": {version, package, tool, args, digest, memory_matches_disk}, "result": …}`: `digest` fingerprints the OPEN connection's canonical form (the state the rows came from); deterministic (no timestamp — same state + args = same bytes); the result carries metadata only (`count`/`total`/`partial` — the file has no payload cap: pass a `limit` above `total` for a whole family). A script quotes from the file, never from `data/*.jsonl` and never from a pasted display; export immediately before generating For row results the FILE's envelope also carries `count`, `total` and `partial`, so a reader need not re-derive a short export (plan 067) |
| `handoff_emit(target_dir, subdir?, force?, refresh_stock?)` | mutate | v3 (plans 027-029): **pure target wiring** — `.mcp.json` (omitted on plugin-hosted servers) + the `CLAUDE.md` operating note as a TOOL-OWNED marker span (`tamheed:note v3`, recording-obligations table; rebuilt every emit — hand edits inside the markers are overwritten with a warning, operator content lives outside them; v1 notes warned, never touched). NO prompt copies — `<package>/prompts/` is the single source (`subdir` refused; leftover `handoff/prm-*.md` get per-file copy-vs-live verdicts). Requires ≥1 project-authored prompt file; G-INJECT + the stale + restated-state scans run over every package prompt file; refreshes the stock library — v4.1 (plan 032): diverged stock is CLASSIFIED against the bundled stock history (`stale-stock`, byte-equal to an older release and named with it, vs `customized`); `refresh_stock=true` safely overwrites ONLY stale-stock (runs before the screens); `force` = overwrite ALL diverged incl. customized; per-file acceptance = delete + re-emit; reports `converted_prompts` curation hints, `stale_references`, `restated_content` Each `diverged_customized` entry also reports `stock_merged` - `"declared X.Y.Z"` when the file carries `<!-- tamheed:stock-merged X.Y.Z -->`, the operator's claim reported as a claim - and `contains_current_stock`; a file leaves the lag warning when either holds (plan 078). The note tags a still-Approved lesson that points at a successor (plan 075) Names, in `warnings`, the feedback rows still awaiting the operator (Proposed) and the confirmed rows not yet exported — ids only; nothing a feedback row says reaches an emitted file (plan 087) |
| `package_migrate(name, confirm?)` | staged | Convert a v2/v3 store to v4 IN PLACE (plan 031): preview = the full rewrite report (every value coercion, edge retype, column drop — nothing written); `confirm=true` = backup to `data-v3-backup/`, legacy `prompts.jsonl` conversion, transform, store-validated canonical write-back, a `system:migrate` audit event. A **v4** store missing baseline entity types gets the staged **registry-sync** instead: preview reports `mode: "registry-sync"` + `entity_types_added` + `columns_added` (columns the live DDL added since the store was last written — those files re-serialize to the current canonical form; the note says so instead of claiming only the registry changes), confirm appends the registry rows + the audit journal row (no backup taken). A third sync reason (v4.5, findings_22 §4): a foreign `*.jsonl.converted` audit-trail file in the canonical `data/` is previewed as `relocate` and, on confirm, moved into `data-v3-backup/` — or removed when the backup already holds a byte-identical copy, or refused naming both paths when they differ (nothing is overwritten); the action text states what was verified (the byte-identical copy) and what was not (that directory's durability — operators commonly gitignore it; if `data/` is git-tracked the file also lives in history; the server never calls git — v4.6, findings_23 §3). Refuses a 4.x store only when its registry is current AND nothing is foreign; refuses leftover backups on the v3 path. `package_open` refuses pre-v4 stores and names this tool. (v1 Keystone trees: two-step route via tamheed 3.2.1 — `docs/migrate-from-keystone.md`) The read-only PREVIEW does not take the writer lock (plan 063): it diagnoses before it refuses and says when it read under a held lock; `confirm=true` still needs the lock |
| `package_adopt(source_dir, name?, confirm?)` | staged | Adopt a brownfield repo (scan/preview, then `confirm`); emits the prompt library on success |
| `package_verify(name?, record?, expect?)` | read (+1 journal row on `record`) | The integrity instrument as a tool (v4.5, findings_22 §5): the canonical round-trip of the ON-DISK store — `store.load` → `store.dump` into a scratch dir → per-file byte-equality (`dirty` names any non-canonical file), `foreign` (files in `data/` the engine does not own), `loadable` (an FK/CHECK/JSON failure is a finding with file:line, never an exception), `memory_matches_disk` when the package is open (a refused flush is exactly when a disk-only round-trip would lie), and `digest` = sha256 over the sorted (name, sha256) pairs of the canonical files. Read-only: no lock, nothing written; with no package open, `name` is required. `record=true` (open package, passing verification only) appends ONE `integrity-verified` journal row (actor `system:package-verify`) naming the digest — the entry states that recording rewrites the journal file, so the next digest differs by construction. Tamper-evidence proper is out of scope `expect=<digest>` adds `matches_expected` — "is this export/slate still current" as a boolean (a mismatch means STALE, not damaged; plan 067). `foreign_csv` names what the exporter does not own in `csv/` (plan 065) `review_current` - `true`/`false` against the digest `export_html` stamped into `review.html`, `null` with no page or no stamp (plan 081) |
| `export_html(output?)` | export | Render the HTML review surface to `<package>/review.html` — dark maximalist identity, sticky TOC, the layered **traceability flow** (plan 027: Needs→Decisions→Work→Verification lanes, labeled clickable nodes, arrowheads, CSS-only relation filters) + the connected-only relations graph (isolated entities folded, degree-scaled radii, 12-hue palette), per-phase readiness panel (latest-verdict semantics) + declared human gates, all tables folded with per-table `csv/<table>.csv` download links (deterministic; `csv/` is DERIVED — hand edits are overwritten on re-export, while authored emissions remain protected; formula-shaped text cells are quote-prefixed in the CSV only — CWE-1236, plan 050), ids ordered numerically within a family (`DEF-9` before `DEF-10`; plan 057), wrap-in-place text, honest freshness — zero JS throughout `csv/` equals what was just emitted (plan 065): a CSV for a table that became empty or that a migration retired is removed (`csv.removed`) — only a regular file in the PACKAGE's own `csv/` whose header proves the exporter wrote it; an operator's file, anything in a caller-chosen `output` directory, and symlinks are reported under `csv.unowned` and never touched The page carries `<meta name="tamheed-digest">` - the digest of the state it rendered - so a stale page is detectable without rendering (plan 081) Since plan 096 the page carries a **Readiness** section (the package-scope rules with status, severity, population, discriminating, omitted, waived, under an *Evaluated as of <date>* line — two rules read the calendar) and a **Feedback** section (awaiting the operator's word / confirmed, not yet reported / registered local tools / closed); the Approved-lessons fold shows the supersession tag; the waivers fold marks an open-ended whole-rule waiver |

Entity `type` values mirror the `entity_types` registry (`requirement`, `decision`, `adr`,
`risk`, `phase`, `slice`, `acceptance-criterion`, `deferred-work`, `lesson`, `skill`, …) plus
two write-only surfaces: `trace-edge` (relations) and `omission` (G-SET recorded-omitted,
with reason).

## HTML review surface (plan 012)

`export_html()` renders the open package's **only human review surface** (D-REVIEW: HTML,
never derived Markdown) to `<package>/review.html` — five sections: overview with per-gate
chips, per-family registers (with `last_referenced` and the three-axis status columns),
the traceability matrix, execution progress (AC × audit verdicts, progress log, scope
changes), and gap/screening notes. The file is self-contained static HTML (embedded CSS,
restrictive CSP, zero JavaScript, zero data-derived links — every data string is escaped
at render time) and deterministic (same DB state ⇒ byte-identical output), so **commit it
to the package's repo**: its diffs are meaningful and reviewers open it without running
anything. Every section carries a freshness stamp derived from the package's own stored
timestamps, never the wall clock.

## Contract

The tool surface is a public contract: additive changes are MINOR, breaking changes are
MAJOR + migration note (governance versioning applies to tools). Handlers are plain
functions — `tests/test_mcp_contract.py` drives them in-process with no transport.

## scratch_diff — field-level package comparison

`python <plugin>/scripts/scratch_diff.py <a>/data <b>/data` (add `--json` for machine
output) diffs two packages row-by-row with correct per-table keying (`trace_edges` on
from/to/relation, `entity_types` on `type_id`, …) and the union of columns INCLUDING
JSON blobs (`custom_attributes`) — a diff that skips blobs silently under-reports
(field-evidence C28). Exit 1 (differences) is a report, not a failure; bucket every
difference as expected or UNEXPECTED — an empty UNEXPECTED bucket is the pass
criterion. Standard uses: comparing a package against its `data-v3-backup/`, or a
re-recorded scratch package against the live one (delete the scratch afterwards — it
is a measurement, never a candidate).
