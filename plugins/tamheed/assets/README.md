# Assets

The Tamheed brand marks (paving-steps motif, bilingual Tamheed / تمهيد wordmark),
maintainer-selected. Four SVGs: `logo.svg`, `logo-dark.svg`, `logo-light.svg` (full
lockups per background), `icon.svg` (the mark alone, used as the plugin icon).
Consumed by the repo README; not read at runtime.
