# Plan 135: tag v5.2.0, the brief file, close-out

> Maintainer-executed, 2026-09-26. Batch map: [129-135-batch-findings-33.md](129-135-batch-findings-33.md).

## Status

- **Priority**: P1 - **Effort**: S - **Risk**: LOW

## What this plan does

1. The close-out commit carries the batch record (EXECUTED, "tagged on the release commit"), the
   brief file [briefs/acmp-5.2.0.md](briefs/acmp-5.2.0.md), the plans index and this file.
2. Push; wait for CI on that commit; tag `v5.2.0` on it; push the tag. Nothing lands after the tag
   (E1 of the 5.1.0 brief: the field installs marketplace HEAD, so HEAD and the tag must agree).
3. The memory file `tamheed-v520-findings33-batch.md` + the MEMORY.md pointer (outside the repo).

## The brief (R9)

A committed file read by path, never pasted: the 5.1.0 brief's fourteen errors owned; the upgrade
with the tag check; ten predicted classes measured on a read-only copy of the ACMP package with the
shipped engine; the three feedback dispositions; the carried-rules verdict (36 covered / ~30 absorbed
/ 33 project-specific) for the operator's per-lesson call; the close-out order; `findings_34`'s
questions. The ACMP prompt is one line naming the path.

## Owed housekeeping (the user's)

- The six orphan worktrees under `.claude/worktrees/` (from the 5.1.0 cycle) are still there.
- The ACMP session runs the brief; the operator confirms, trims or rejects `LL-099`–`LL-111`.
