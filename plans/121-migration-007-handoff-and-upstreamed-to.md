# Plan 121: migration 007 — the `handoff` journal kind + `skills.upstreamed_to`

> Maintainer-executed, 2026-09-26. Batch map: [120-128-batch-findings-32.md](120-128-batch-findings-32.md).

## Status

- **Priority**: P1 - **Effort**: S - **Risk**: LOW (CHECK recreation on an empty-at-connect table +
  one nullable column; the one-time `skills.jsonl` rewrite is predicted)

## Why this matters

Note 4 (findings_32): a project carried "where the session stopped" in prose `note` entries and a
3,594-line kickoff prompt. A typed kind gives the resume block (plan 122), the hook (123) and the
`handoff-current` advisory something to read. FB-020: `skills.superseded_by` is an FK to `skills(id)`
and cannot name a plugin skill, so a retired project skill whose twin is `tamheed:<name>` stranded its
Promoted lessons; `upstreamed_to` is the pointer the `lessons-stranded` advisory (122) passes on.

## What changes

`db/migrations/007_handoff.sql` (the 004/006 pattern; `ALTER TABLE skills ADD COLUMN upstreamed_to
TEXT`); `PE_EVENT_TYPES` += `handoff` (caller-appendable); the `progress_update` docstring;
`test_store_migrations.test_migration_007_handoff_lands`; `test_mcp_contract` head/version asserts →
`007_handoff.sql` / 7; `docs/entities.md` PE column row; `references/artifact-catalog.md` PE row;
`server/README.md` `progress_update` row; `references/extension.md` (migration list + the recipe's
caller-written example); CHANGELOG `[Unreleased]` names the migration (lint 7); the four fixtures'
`skills.jsonl` regenerated by `store.load` + `store.dump` (CANONICAL rule 4: every column serialises,
so `"upstreamed_to": null` lands on every row once).

## Done criteria

- [ ] `python check.py` green (suites, lint 7/9, canonical after the regolden, evals)
- [ ] `server_info` reads `schema_version 7`, `migrations_head 007_handoff.sql`
- [ ] `git diff --stat` on the fixtures shows only `skills.jsonl` files, each row gaining the one key
