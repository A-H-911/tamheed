# Plan 124: the skills — session-handoff, the resume step, five practices, two absorbed steps, the retirement doctrine — and the AGENTS template

> Maintainer-executed, 2026-09-26. Batch map: [120-128-batch-findings-32.md](120-128-batch-findings-32.md).

## Status

- **Priority**: P1 - **Effort**: M - **Risk**: LOW (prose in teaching files; lint 12 guards
  stack-neutrality and field identifiers)

## Why this matters

Note 4 needs a skill that says when and how to write the handoff; note 3 named five generic
practices living only in one project's memory; FB-019 named two integrity-check steps the stock
never had; FB-021 asked how a distilled skill that contradicts its lesson is corrected; FB-020 asked
how a project skill retires behind a plugin twin; note 2 found the AGENTS template promising a
regeneration nothing performs.

## What changes

- **New** `skills/session-handoff/SKILL.md` (menu + model; no flag — lint 12 exempts it): when
  (before a compaction, at session end, on a handover, when the resume block says behind); the
  five-section shape (resume at / in flight / awaiting the operator / verified facts with their
  instrument / do not carry); written LAST; ids not rows; a stale one corrected via `corrects`;
  nothing instruction-shaped (the hook screens it); commit the data.
- `orient-resume`: step 0 reads the resume block and the handoff first; after a compaction
  `server_info` is the first call (the package is still open) and step 2 is skipped.
- `package-writes` §11: never manufacture a status; a global rename skips records that quote the
  old text (sweep with `search=` across families).
- `reading-the-record` step 7: a carried premise is a claim — the carried pick, the capability
  judged from one class, the cost carried as inherent (three checks, anonymised); the ADR amend
  list checked against every row quoting the subject (step 4 already carried the instrument).
- `written-claims` step 7: fix the file, never annotate the pointer (and the distilled-skill case).
- `operator-interview` "What you cannot see": a permission prompt never appears in a tool result
  (ask the operator to name the tool); ask before anything destructive or outward.
- `integrity-check` step 8 gains the `changed_columns` re-derivation (and the no-projection read,
  the stash/export sources); new step 9 lessons liveness; the report step renumbered.
- `skill-promote` "After the ceremony": mechanism correction = hand-edit + `correction` entry
  naming the `SKL-` row (re-distillation only when the lesson SET changes); retirement behind a
  plugin twin = `Obsolete` + `upstreamed_to` (`lessons-stranded` passes; the note line drops it);
  `handoff_emit`'s stale scan over the skill files (plan 125).
- `templates/agent-control.template.md`: frontmatter keeps `owner` only (with the reason); the
  handoff sentence under the obligations table (the table itself unchanged — note v5); "Where you
  are now" names the resume block and the restated-content report.

## Anonymisation rule applied (lint 12)

`LL-NNN`/`SKL-<next>` shapes only; no field ids, findings numbers, project or stack names.

## Done criteria

- [ ] `python check.py` green (lint 12 incl. the menu contract; the roster test)
- [ ] 25 skills; the model listing shows 9 (probe in plan 127)
